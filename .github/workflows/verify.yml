name: Verify

on:
  push:
  pull_request:

jobs:
  verify:
    runs-on: ubuntu-latest
    steps:
      - name: Check out repository
        uses: actions/checkout@v4
      - name: Verify Codex Orchestration package
        run: sh plugins/codex-orchestration/scripts/verify.sh
