Use $codex-orchestration:orchestration for this release smoke task.
Repair the deliberately seeded invalid-input exit-status bug in spawn-audit.py. Add a standard-library subprocess regression test: malformed JSON input must produce a JSON error and process exit status 1. Demonstrate the test fails before the repair and passes afterward. The public CLI is the agreed test seam; no typechecker is configured.
Delegate the repair, treat its Consequence as normal, and obtain a fresh review. Use only the native spawn tool for delegation. Commit the candidate on this repository's current branch. Do not push or modify files outside this disposable repository, except write smoke evidence to /tmp/codex-issue14-74o2zfxo/evidence.
Retain the plan and acceptance reports, validation results, spawn-audit input/output and the HEAD/status/diff observations before and after review and at Acceptance in /tmp/codex-issue14-74o2zfxo/evidence. Use the installed plugin's spawn-audit command to audit your own live session. If native pinned spawning is unavailable, report the smoke failure and stop; do not substitute a Parent-only run. Do not ask optional questions: the task and seam above are settled.
